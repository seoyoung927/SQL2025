SELECT 
    B.ID AS ID,
    B.GENOTYPE AS GENOTYPE,
    A.GENOTYPE AS PARENT_GENOTYPE
FROM ECOLI_DATA A, ECOLI_DATA B -- A: 부모, B: 자식
WHERE A.ID = B.PARENT_ID
AND A.GENOTYPE & B.GENOTYPE = A.GENOTYPE
ORDER BY ID ASC;