SELECT 
    B.ID AS ID,
    B.GENOTYPE AS GENOTYPE,
    A.GENOTYPE AS PARENT_GENOTYPE
FROM ECOLI_DATA A -- 부모
INNER JOIN ECOLI_DATA B -- 자식
ON A.ID = B.PARENT_ID
WHERE A.GENOTYPE & B.GENOTYPE = A.GENOTYPE
ORDER BY ID ASC;