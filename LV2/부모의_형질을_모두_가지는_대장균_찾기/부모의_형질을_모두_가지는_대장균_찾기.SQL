SELECT
    B.ID,
    B.GENOTYPE,
    A.GENOTYPE AS PARENT_GENOTYPE
FROM ECOLI_DATA AS A -- 부모
JOIN ECOLI_DATA AS B -- 자식
ON A.ID = B.PARENT_ID
-- 부모와 나의 GENOTYPE을 계산했을 때 부모의 GENOTYPE을 모두 가짐
WHERE A.GENOTYPE & B.GENOTYPE = A.GENOTYPE
ORDER BY ID ASC;